object 객체
프로퍼티, 메소드의 집합
dog {name:"해피", age:3} //name, age = 프로퍼티

이벤트 리스너
onmouseover/onmouseout: 해당태그에 마우스가 올라갔을때/내려갔을때

함수
특별한 목적의 작업을 수행하도록 설계
필요할 때마다 호출하여 해당 작업을 반복하여 수행

반환 (return)
- 배열이나 객체를 포함한 모든 타입의 값을 반환

묵시적 타입 변환
10+"문자열" - 문자열 연결
"3"*"5"; - 곱셈 연산을 위해 두 문자가 숫자로 변환
1-"문자열"; NaN

NaN Not a Number
0으로 나눔, 숫자로 변환 불가한 피연산자로 산술연산을 시도하는 경우에 반환되는 읽기 전용

Number()
String()
Boolean()
Object()


변수: 데이터를 저장할 수 있는 메모리 공간으로 값이 변경될 수 있음

var 키워드를 사용하여  변수 선언 (javascript)
선언되지 않은 변수를 사용하거나 접근하려고 하면 오류 발생
선언되지 않은 변수를 초기화할 경우는 자동으로 선언을 먼저 한 후 초기화 진행 (javascript)

변수 선언 후 초기화/선언과 동시에 초기화
쉼표로 여러 변수를 동시에 선언
재선언 무시

산술연산자 +,-,*,/,%
대입연산자 =,+=,-=,*=,/=,%=
증감연산자 ++,--
비교연산자 <,>,<=,>=,==,!=

=== 값이 같고 타입이 같음 (javascript)
!== 값이 같지 않거나 타입이 같지 않음 (javascript)

논리연산자 &&, ||, ! and, or, Not


제어문 프로그램의 순차적인 흐름을 제어할 때 
조건문
if문(if/else문, if/else if/else문), switch문

if(표현식): 표현식의 리턴값이 참일때
else: if 표현식 결과가 거짓일때
else if(표현식2): 표현식2가 참일때
else 나머지
